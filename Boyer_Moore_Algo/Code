class Solution {
    public int majorityElement(int[] nums) {
        int c = 0, m = 0;
        for (int x : nums) {
            if (c == 0) {
                m = x;
                c = 1;
            } else {
                c += m == x ? 1 : -1;
            }
        }
        return m;
    }
}
